<% if @user.errors.any? %>
<div id="erorr_expanation">
	<div class="alert alert-danger">
		The form contains <%= pluralize(@user.errors.count,"error") %>
	</div>
	<ul>
		<% @user.errors.full_messages.each do |mag| %>
		<li>
			<%= mag %>
		</li>
		<% end %>
	</ul>
</div>
<% end %>
